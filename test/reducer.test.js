import reducer from "../client/src/js/reducer";

describe("filter list reducer", function () {
    it("correctly produces default state when passed one is undefined", function () {
        expect(1).toEqual(1);
    });
});